# Studio truthful runnable anchor workflow hardening

This patch hardens the single validated runnable Studio workflow without expanding runtime support.

## Frozen truthful line

- The exact validated runnable anchor remains the Closed Box seed motif.
- Bass Reflex remains a seeded but gated path.
- Transmission Line and Horn remain seed-only and non-runnable.
- Structural graph edits are allowed, but runtime truth must follow the compiler-boundary assessment immediately.

## What this patch adds

- clearer factual wording for the current anchor state
- stronger UI distinction between:
  - validated Closed Box anchor
  - seeded but gated paths
  - composition mode beyond current compiler truth
  - invalid graph state
- reset wording that explicitly restores the exact stored seed for the current topology
- tighter alignment between Run Simulation affordance and current assessment truth

## Operational rule

The UI must never imply that a graph remains on the validated runnable anchor after structural edits or constraint violations.
